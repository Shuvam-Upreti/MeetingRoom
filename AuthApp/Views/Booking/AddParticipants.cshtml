@using MeetingRoom.ViewModel
@model ParticipantVModel

@{
    ViewData["Title"] = "AddParticipant";
}
<partial name="_Notification" />
<h1>AddParticipant</h1>

<h4>Participants</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post" asp-action="AddParticipants">
             @* <input type="hidden" asp-for="BookingId" />  *@
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserId" class="control-label"></label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.userList" >
                    <option value="">SelectUsers</option>
                </select>
            </div>  
            <div class="form-group">
                <label asp-for="BookingId" class="control-label"></label>
                <select asp-for="BookingId" class="form-control" asp-items="ViewBag.bookingList" disabled>
                    @* <option value="">----</option> *@
                </select>
            </div>
           
           @*  <div class="form-group">
                <label asp-for="BookingId" class="control-label"></label>
                <label asp-for="BookingId" class="form-control">@ViewBag.bookingList</label>    
            </div> *@
            <div class="form-group">
                <input asp-controller="Booking" asp-action="AddParticipants" type="submit" value="Create" class="btn btn-primary" />
                <a class="btn btn-primary" asp-action="Index">Back to List</a>
                @* <button type="submit">AddParticipant</button> *@
            </div>
        </form>
    </div>
</div>

<div>
</div>

